# Do not edit this file directly,
# Edit routes.yml to modify these settings
http:
  routers:
    databasus:
      rule: "Host(`databasus.lab.ehcan.io`) || Host(`db-backup.lab.ehcan.io`)"
      entryPoints:
        - websecure
      service: databasus
  services:
    databasus:
      loadBalancer:
        servers:
          - url: "http://10.10.0.102:4005"
