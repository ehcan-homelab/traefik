# Do not edit this file directly,
# Edit routes.yml to modify these settings
http:
  routers:
    notes:
      rule: "Host(`notes.lab.ehcan.io`)"
      entryPoints:
        - websecure
      service: notes
  services:
    notes:
      loadBalancer:
        servers:
          - url: "http://10.10.0.102:9900"
