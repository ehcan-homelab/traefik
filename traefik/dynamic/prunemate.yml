# Do not edit this file directly,
# Edit routes.yml to modify these settings
http:
  routers:
    prunemate:
      rule: "Host(`prunemate.lab.ehcan.io`)"
      entryPoints:
        - websecure
      service: prunemate
  services:
    prunemate:
      loadBalancer:
        servers:
          - url: "http://10.10.0.102:7676"
