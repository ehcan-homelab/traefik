# Do not edit this file directly,
# Edit routes.yml to modify these settings
http:
  routers:
    truenas:
      rule: "Host(`truenas.lab.ehcan.io`)"
      entryPoints:
        - websecure
      service: truenas
  services:
    truenas:
      loadBalancer:
        servers:
          - url: "https://192.168.2.149"
        serversTransport: insecureSkipVerifyTransport
