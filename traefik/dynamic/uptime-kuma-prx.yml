# Do not edit this file directly,
# Edit routes.yml to modify these settings
http:
  routers:
    uptime-kuma-prx:
      rule: "Host(`uptime.lab.ehcan.io`)"
      entryPoints:
        - websecure
      service: uptime-kuma-prx
  services:
    uptime-kuma-prx:
      loadBalancer:
        servers:
          - url: "http://10.10.0.102:3001"
